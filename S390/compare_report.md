# Compare Report

This report compares pre-feature (Project A) and post-feature (Project B) search implementations across the standard test set.

Run `run_all.sh` to re-run tests and regenerate this report.

Summary metrics include pass rate, average latency, and per-test comparisons. The `results/aggregated.json` file contains the full structured metrics.

